# Slack App Credentials (REQUIRED)
# Get these from https://api.slack.com/apps/YOUR_APP_ID
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_APP_TOKEN=xapp-your-app-token

# Database (REQUIRED)
# PostgreSQL connection string
DATABASE_URL="postgresql://username:password@localhost:5432/trace?schema=public"

# App Configuration (OPTIONAL)
PORT=3000
NODE_ENV=development

# Security Configuration (OPTIONAL but RECOMMENDED)
# Restrict app to a specific workspace
# Get your workspace ID from: https://api.slack.com/methods/team.info/test
# ALLOWED_WORKSPACE_ID=T0123456789

# Rate Limiting (OPTIONAL)
# Customize rate limits if needed (defaults: 60 requests/minute, 5 min block)
# RATE_LIMIT_MAX_REQUESTS=60
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_BLOCK_DURATION_MS=300000